services:
  qsl_rs:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/query-store-links/qsl_rs:latest
    ports:
      - "5236:5236"
    environment:
      HOST: "0.0.0.0"
      PORT: "5236"
      ALLOWED_ORIGINS: "https://*.krnl64.win"
      # Uncomment for local development (allows localhost origins):
      # DEV: "true"
      LOG_LEVEL: "info"
    restart: unless-stopped
